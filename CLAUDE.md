# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**Finderella** — a Plex/Jellyfin-style media streaming web app (movies + series) with a **distributed backend**: the SvelteKit app ("hub") runs on a storage-poor VPS; media lives on the user's other devices, each running a Node storage gateway — the media agent (`packages/storage-gateway`, bin `finderella-storage-gateway`) — that dials out to the hub over one WebSocket and serves file ranges + ffmpeg HLS transcodes through that tunnel. Dark cinematic theme (forced dark, teal accent).

### Built so far

- **Pages**: Home (hero banner + landscape carousels), `/movies` & `/series` libraries (portrait grids, genre/sort filters), `/categories` (row per genre), detail pages (`/movies/[id]`, `/series/[id]` with season tabs + episode carousel), watch pages (`/movies/[id]/watch`, `/series/[id]/watch/[episode]`), styled 404.
- **Search**: instant, typo-tolerant search over movies + series. **MiniSearch** index in process memory (`src/lib/server/search/`: `engine.ts` is the pure, tested ranker — fields `title`×8, `yearText`×3, `people` (director/creator + cast)×3, `tagline`×1.5, `synopsis`×1; prefix on every term, fuzzy 0.2 (max 2 edits) only on non-numeric terms longer than 3 chars, exact-title bonus, AND first then OR fallback, `normalizeTerm` strips diacritics on index **and** query; `index.ts` is the singleton — `searchCatalog`, `invalidateSearchIndex` (mark stale → rebuilt on the next query, single-flight; called from `finalizeScan`, `pruneCatalog` and the end of `enrichPending`) and `warmSearchIndex` (boot `init` hook)). `GET /api/search?q=&limit=` (`src/routes/api/search`, ≤50) returns `SearchResult[]` (`src/lib/data/search.ts`: kind/slug/title/year/posterUrl/genres/score; auth is the hook's — 401 when login is required, open in guest mode). Navbar `SearchBox` (`src/lib/components/search-box.svelte`, desktop `dropdown` + mobile-sheet `inline` layouts): 150 ms debounce, `AbortController` + sequence guard against out-of-order responses, hand-rolled `role=combobox`/`listbox` with ↑/↓/Enter/Esc, poster thumb + Movie/Series `Badge` per row, "See all results" footer; Enter with no row highlighted → **`/search?q=`** (`searchHref`), whose loader hydrates the ranked slugs via `getMoviesBySlugs`/`getSeriesBySlugs`, re-applies the hit order and renders `PosterCard showKind` in a grid. Library pages no longer read `?q=` (genre/sort filters only).
- **Data layer**: pages read the catalog from Postgres via server loads calling `src/lib/server/catalog.ts` (async queries returning the exact `src/lib/data/types.ts` shapes; public IDs are slugs). `src/lib/data/` keeps: `types.ts` (the frontend contract), `hrefs.ts` (client-safe `mediaHref`/`watchHref`/`episodeWatchHref` via `resolve()`; `watchHref` sends a series to its next-in-line episode), `episodes.ts` (pure `flattenEpisodes`/`playTarget`/`episodeLabel` — `playTarget` reads `Series.lastWatchedEpisodeId` + episode `progress`, stamped by `applyProgress`, to pick resume-latest-else-next; heroes label it `Resume S2E4`), and pure helpers (`topRated`/`byGenre`/`allGenres` — items passed in). There is no mock/seed corpus: titles only come from gateway scans, and `catalog/prune.ts` removes titles left without any `media_file` (after each scan, and via "Remove titles without files" in settings). Artwork (`PosterArt`) is a flat `bg-muted` box with the title + year centered until the `posterUrl`/`backdropUrl`/still is **fully** loaded, then the image fades in over 100 ms (`LoadedImage`: `opacity-0` until `load`/`complete`, `decoding="async"`; a failed load keeps the placeholder). Titles without artwork keep the gray box permanently. `hue`/`hue2` are still stored and typed (`theme`) but no longer rendered.
- **Backend** (Phases 0–4 built & verified): npm workspaces — `packages/protocol` (zod message schemas + shared language table + binary framing: 6-byte header `UInt32BE requestId | flags(FIN) | reserved`, hub ids odd / gateway even), `packages/storage-gateway` (CLI: `pair`, `connect`; ffprobe scanning, credit-gated file transfers, ffmpeg HLS transcode sessions; resolves ffmpeg/ffprobe from env → system PATH → bundled `ffmpeg-static`/`ffprobe-static`). Hub: `src/lib/server/gateways/` (WS auth by per-gateway token sha256; `registry` = connection map + request correlation + `openByteStream` with credit-based backpressure), `src/lib/server/streaming/` (compat matrix, source picker, session manager, hub-synthesized VOD HLS playlists), `src/lib/server/catalog/` (filename→catalog ingest + parser + orphan prune), `src/lib/server/metadata/` (TMDB provider: `tmdb.ts` client + zod schemas, `map.ts` pure genre/maturity/match mapping, `index.ts` `enrichPending` single-flight runner triggered after scans, at boot, and by "Refresh all metadata" in settings), `src/lib/server/progress.ts` (watch positions, continue-watching, `loadProgress`/`applyProgress`/`withProgress` stamp a 0–1 `progress` **and `inWatchlist`** onto items + episodes in every browsing loader and the series watch loader (its `show` feeds the player's episodes panel); thresholds in client-safe `src/lib/data/progress.ts`; `dismissProgress` sets `watch_progress.dismissed_at` so a title leaves "Continue watching" until the next `saveProgress` clears it; `markWatched` writes a finished position for a movie / every episode of a series — one-way, no unwatch). **Watchlist** (`src/lib/server/watchlist.ts`, `watchlist` table with the same partial unique indexes as `watch_progress`): `POST /api/watchlist {kind, slug, inWatchlist}`, `/watchlist` page (`LibraryPage` with a "Recently added" default sort + empty state; in `ACCOUNT_PREFIXES`), navbar **Watchlist** link left of the avatar, and a ⋮ menu (`card-menu.svelte`, top-left of every `PosterCard`, signed-in only, always visible on touch) with Add/Remove (Show) to Watchlist and — on the home Continue-watching row only (`MediaRow continueWatching`) — "Remove from Continue watching" (`POST /api/progress/dismiss`). Detail heroes: an oversized Play/Resume (movies: `Resume at 15m 25s` — elapsed units, never clock-style, which reads like a time of day — from `movieWatchState` passed as `DetailHero resume`, with a bare progress line under the poster; `formatDurationUnits` in client-safe `src/lib/data/time.ts`), then Watchlist toggle / **Mark as watched** (`POST /api/progress/watched`; disabled "Watched" once `progress === 1` for the movie / every episode) / Watch trailer; browser calls live in `src/lib/watchlist-client.ts`. Movie detail pages show the source resolution (`getMovieDetail` → `Movie.sourceWidth/Height` from the best active `media_file`, rendered by `MetaPills` via `resolutionLabel`, "4K" for 2160p; list queries and series don't carry it). Auth gate lives in `hooks.server.ts` (redirect to `/login`; public: `/login`, `/register`, `/api/auth`, `/api/gateway/pair`; `/api/stream/*` short-circuits Better Auth entirely — stream URLs authorize by unguessable playback-session uuid). **Roles**: Better Auth `admin()` plugin (`user.role` = comma-separated string; helpers in client-safe `src/lib/auth-roles.ts`: `isAdmin`, `roleForNewUser`, `isRegistrationOpen`). The first account on an empty hub becomes `admin` (app-level `databaseHooks.user.create.before` in `auth.ts`); later sign-ups are `user`; `ensureAdminExists()` (`src/lib/server/users.ts`, run at boot) promotes the oldest account on installs that predate roles. **Admin dashboard** at `/admin/*` (shadcn `sidebar` layout, no navbar): Overview (counts), Devices (`/admin/devices`: pairing codes, libraries, rescan, rename/revoke/remove), Users (`/admin/users`: list/create/promote/demote/ban/unban/delete via `auth.api.*` admin endpoints), Subtitles (`/admin/subtitles`: provider keys, languages, auto-download, bulk job + history), Site settings (`/admin/settings`: **Require an account to access** (`site_settings.require_login`, default on) and **Allow visitors to register** (`allow_registration`) — independent switches posted from one form; a **Trickplay** card with the seek-bar thumbnails switch (`trickplay_enabled`, its own form/action so it can't blank the Access switches); catalog prune, metadata refresh + TMDB attribution). Navbar (`site-header.svelte`, shown only when signed in and outside `/admin`) has a far-right avatar dropdown with **Settings**, **Admin dashboard** (admins) and **Sign out** (`POST /logout`). **`/settings`** (any signed-in user): display name (`auth.api.updateUser`), email (direct `user.email` update — no verification mail exists, and Better Auth's `changeEmail` refuses verified users) and password (`auth.api.changePassword` with `revokeOtherSessions`); holds the per-user subtitle defaults (Subtitles card); playback defaults can join them later. `/register` asks only for email + password (`displayNameFromEmail` in `src/lib/server/users.ts` seeds the name); password fields use `src/lib/components/password-input.svelte` (show/hide toggle; the two eye icons are rendered in an `{#if}` because `HugeiconsIcon` draws its `icon` prop once on mount and ignores later changes). Watch pages create sessions client-side via `POST /api/playback/start` → `{mode: direct|hls, src, sessionId, quality}` (optional `quality` from the ladder in `src/lib/playback-quality.ts`: `original` = direct play when compatible else source-res transcode (≤3840 wide); `2160p|1080p|720p|480p|360p` force HLS with `-maxrate` caps for slow remote uplinks; responses carry `source.{width,height}`) (404/503 with a readable message when the title has no file / its device is offline); progress reports throttle to `/api/progress`; resume via `startAt`/`startSeconds`.
- **Media components** (`src/lib/components/media/`): `Carousel` (snap scroll, edge fades, hover arrows; `gutter` prop defaults to `page-gutter` — override for embedded surfaces), `PosterCard` (poster/backdrop variants), `LoadedImage` (an `<img>` that stays invisible until fully loaded — no wrapper element, so `[&>img]:` overrides on its parent still apply; also used for cast headshots), `MediaRow`, `HeroBanner`, `DetailHero` (Play/Resume + optional Watch trailer), `TrailerDialog` (YouTube embed in a `ui/dialog`, iframe mounted only while open), `ProgressLine` (3px watch-progress bar under `PosterCard`/`EpisodeCard`; always reserves its height so titles align), `EpisodeCard` (always-visible corner play badge; `active` shows a teal equalizer "now playing" state, `onNavigate` callback), `LibraryPage`/`LibraryToolbar`, `WatchPlayer`, `EpisodesPanel`.
- **Subtitles**: discovered at scan time by the gateway and stored per file in `media_subtitle` (child of `media_file`, cascade; rows replaced on every scan that reports the file — gateways that predate subtitles omit `ProbedFile.subtitles` and keep their rows). **Embedded** text streams (`subrip`/`ass`/`ssa`/`webvtt`/`mov_text`/`text`, `packages/storage-gateway/src/subtitles/embedded.ts`) come from the same ffprobe JSON the scan already runs, stored with ffprobe's _absolute_ stream index; bitmap streams (PGS/VobSub/DVB) are skipped. **Sidecars** (`.srt/.vtt/.ass/.ssa`, `subtitles/sidecar.ts`, pure + tested) are matched from the video's folder, a `Subs`/`Subtitles` child folder and its immediate subfolders: name-prefix match (`Movie.en.forced.srt`), `Subs/<video name>/…`, or any file when the video is the only non-`sample` video in its folder; language/flags parse from the leftover tokens (`eng`, `2_English`, `forced`, `sdh`/`hi`/`cc`) via the shared table in `packages/protocol/src/languages.ts`. **Delivery**: `POST /api/playback/start` returns `subtitles: SubtitleTrack[]` (`src/lib/server/streaming/subtitles.ts` labels: "English", "English (SDH)", "French (Canadian)", `Track N`), each pointing at `GET /api/stream/<session>/subtitle/<id>.vtt`, which checks the row belongs to the session's file and proxies a `subtitle.get` tunnel message — the gateway converts everything to UTF-8 WebVTT (`subtitles/extract.ts`: srt/vtt in-process with a windows-1252 fallback for non-UTF-8 files, ass/embedded via `ffmpeg -map 0:<index> -f webvtt pipe:1`), caches the result under `tmpdir/finderella/subs/<hash>.vtt`, and streams the growing `.part` file while ffmpeg is still demuxing (`StreamTransfer`, `stream-transfer.ts`, same credit window as `FileTransfer`). **Player**: a hand-rolled Subtitles menu (right cluster, Off + one row per track; `c` toggles) drives our own `<track>` elements' `.track.mode` (`pickSubtitleTrack` in `src/lib/subtitle-preference.ts`: plain track in the preferred language → SDH → forced → the container's default track; English when nothing is set). **Per-account subtitle settings** (`user_settings` table, `src/lib/server/user-settings.ts` `getSubtitleSettings`/`saveSubtitleSettings` + zod `SubtitleSettingsPatch`; client-safe options/defaults/`normalizeSubtitleSettings`/`cueLine`/`cueStyle` in `src/lib/data/subtitle-settings.ts`; cue size is `SIZE_SCALE` **percent of player height** emitted as `vh` — `::cue` percentages resolve against a small inherited size in Chromium and come out unreadable; the `/settings` preview is a `@container-[size]` 16:9 box that scales the same number by `cqh` and steps position by `1.2em` lines; a track with unknown language (`und`, "Track N") is a session-only pick — `preferenceForTrack` returns null and nothing is persisted, and `normalizeSubtitleSettings`/`SubtitleSettingsPatch` accept only codes the language table knows): preferred language (off or a code), text size, color, background on/off (dark box; off = text-shadow instead), vertical position (0..`MAX_SUBTITLE_LINES` cue lines above the bottom edge → `VTTCue.line = -(n+1)`) and font (sans/serif/mono/casual). Edited on `/settings` (Subtitles card: shadcn `Select`s — the language list gets `max-h-72` so it scrolls — a `Switch` mirrored by a hidden `true`/`false` input, a native `<input type=range>` for position, and a live preview; `?/updateSubtitles`); the watch loaders pass them to `WatchPlayer subtitleSettings`, which sets `cueStyle()` custom properties on `.player-root` for the `video::cue` rule and lifts cues in `positionCues` (on track `load` and `cuechange` — not via the Chromium-only `::-webkit-media-text-track-container` translate, which Firefox ignores). Picking a language in the player menu writes it back to the account via `POST /api/settings/subtitles` (and to localStorage `finderella:subtitles`, which is what guests use).
- **Subtitle downloads** (`src/lib/server/subtitles/`): providers **OpenSubtitles.com** (REST v1: `Api-Key` + `User-Agent: Finderella v0.1`, optional account login → JWT cached 12 h, honour `base_url`), **Subdl** (v1, `api_key` query; results are ZIPs on `dl.subdl.com` unpacked with `fflate`, `zip.ts` picks the episode-tagged/largest text entry), **Gestdown** (keyless Addic7ed/SuperSubtitles proxy, TV only, opt-in switch; show resolved by name via `/shows/search` matched on `tmdbId`, cached a day; `/subtitles/get/{show}/{s}/{e}/{lang}`; raw `text/srt` or a season ZIP) and **Titlovi.com** (free account → `POST /gettoken`, token cached; `GET /search` with display-name `lang` values `Srpski|Cirilica…` from `providers/titlovi-languages.ts`, `type` 1/2, `season=N&season=0` also returns packs; download `titlovi.com/download/?type&mediaid` → ZIP). `providerApplies()` in `search.ts` skips Gestdown for movies and Titlovi for languages it lacks. Every download is re-encoded to UTF-8 hub-side (`charset.ts`: legacy code page guessed from the language — windows-1250 for hr/bs/sr/sl, 1251 for Cyrillic/mk/ru, 1252 otherwise) before `subtitle.put`. Keys live in the `subtitle_settings` singleton (`settings.ts`, edited on **`/admin/subtitles`**: providers + Test connection, languages, **auto-download** switch, prefer-SDH), never in `.env`. `search.ts` builds a `TitleQuery` from the file's movie/series rows (`tmdb_id` when enriched, else title+year; series → parent id + season/episode) and fans out `Promise.allSettled` over configured providers; `rank.ts` ranks in tiers: OpenSubtitles **moviehash match** (+8; `moviehash.ts` computes the hash hub-side from two 64 KiB `file.read` range reads on first use and stores it in `media_file.moviehash`, cleared by ingest when size/mtime change — no gateway change) → same release **group + source** (`parseRelease`: group from the trailing `-GROUP`, source WEB-DL/WEBRip/BluRay/HDTV…, network AMZN/NF/…, resolution, codec family; site tags like `[EZTVx.to]` ignored) → same source → leftover-token overlap, trusted, capped popularity; HI-mismatch/forced/machine/AI translations demoted. `matchReason` (`hash`/`release`/`partial`) rides on each candidate and the player panel shows "exact file match" / "same release". `install.ts` writes the file **beside the video** via the new `subtitle.put` tunnel message (gateway capability `subtitleWrite`; never mkdirs, never overwrites — the hub retries `<stem>.<lang>[.hi].<n>.srt`), names it so `matchSidecars` re-finds it after a rescan, inserts the `media_subtitle` sidecar row (servable at once) and a `subtitle_download` log row; installs are serialized per file. `bulk.ts` is the single-flight job (progress, Stop, per-run provider disabling on quota/auth errors, `not_found` misses skipped for 7 days) behind `startBulkDownload`/`bulkStatus`; `queueAutoSubtitleDownload` runs it for files newer than the last auto run, called at the end of `enrichPending` (or from `finalizeScan` when TMDB isn't configured). Player: **Find subtitles…** row in the Subtitles menu (signed-in + a provider configured → `canFindSubtitles` from the watch loaders; the button shows even with no tracks) opens `find-subtitles-panel.svelte` (language select, ranked results with provider/SDH/forced/AI badges, Download) → `POST /api/subtitles/search` / `POST /api/subtitles/download` (`src/lib/subtitles-client.ts`); the page swaps `playback.subtitles` with the returned list and the player selects the new track.
- **Video player** (`WatchPlayer`): Video.js v10 beta custom elements (`@videojs/html`, exact-pinned) with a fully custom Netflix-style control bar (seek slider w/ hover time, centered play ±10s, volume flyout, captions menu, cast, fullscreen), auto-hide controls + top bar + cursor, keyboard shortcuts (space/arrows). Watch pages are `ssr = false`. Series-only extras (via optional `show`/`currentEpisodeId` props, passed only by the series watch page): "More Episodes" button (left cluster) opening `EpisodesPanel` — a bottom-sheet with season pills + episode carousel; a "Quality" menu (gear icon, right cluster; Original/2160p/1080p/720p/480p/360p filtered by the source width via `availableQualities`, the Original row shows the resolved resolution + direct/transcoded; persisted in localStorage `finderella:quality`; picking a rung makes the watch page restart the session at the current position via `onQualityChange`), an "Autoplay" switch (right cluster, on by default, persisted in localStorage `finderella:autoplay-next`) that autoplays the next episode on `ended` with a "Next episode starting in…" countdown pill over the last 10s. Playback auto-starts whenever the player mounts or is re-sourced (blocked by browser policy on cold loads — caught, stays paused).

- **Trickplay** (seek-bar scene thumbnails): the gateway renders 10×10 JPEG sprite sheets (`packages/storage-gateway/src/trickplay/`: `geometry.ts` pure layout — 400 px tiles every 15 s (`TRICKPLAY_GEOMETRY_VERSION` 2 — bump it whenever interval/width/grid change, it is part of the cache key), tile height from the _display_ aspect (DAR/SAR/rotation), `sheets = ceil(tiles/100)`; `ffmpeg.ts` pure `trickplayArgs` — `-skip_frame nokey` keyframe-only decode, `fps=1/10,tpad=stop=3:stop_mode=clone,scale=W:H,setsar=1[,tonemap],tile=10x10 -frames:v <sheets>` (the `tpad` clones cover the last tile `fps` drops when the keyframe-only stream ends before the probed duration; `-frames:v` keeps the clones from adding a sheet), `-pix_fmt yuvj420p -q:v 5`, image2 `-atomic_writing 1`; `cache.ts` — persistent `cacheDir()/trickplay/<sha256(path|size|mtime|v1)>/sheet-NNN.jpg` + `manifest.json` (written last, with the real sheet count); `queue.ts` — `TrickplayQueue` with two lanes (≤1 `high` = first play, ≤1 `low` = admin bulk), `os.setPriority` low, in-memory failure map, manifest-less dirs wiped as stale partials). Additive tunnel messages `trickplay.ensure {rootPath, relPath, priority}` → `resp data: TrickplayEnsureResult {status ready|generating|queued|failed, geometry}` (idempotent, single-flight per key) and `trickplay.get {…, sheet}` → binary frames (waits ≤20 s for the _next_ sheet while generating, fails fast for ones further ahead; **not** counted against `maxConcurrentTransfers`). Capability `GatewayCapabilities.trickplay` (env `FINDERELLA_TRICKPLAY=0` disables). Hub: `src/lib/server/trickplay/` — `ensure.ts` (`canTrickplay`/`ensureTrickplay` best-effort `registry.request`, `trickplayVttSrc`), `vtt.ts` (pure `buildTrickplayVtt` — hub-synthesized WebVTT with `N.jpg#xywh=x,y,w,h` cues, like the HLS playlist trick), `bulk.ts` (single-flight per-library "Generate thumbnails" job driven file-by-file with `priority: 'low'`, status GET `/admin/devices/trickplay-status` checks `isAdmin` itself). `POST /api/playback/start` calls `ensure` (`high`, 2 s timeout, in parallel with `session.start`/subtitle listing) and returns `trickplay: { vttSrc } | null`; the hub-wide switch is `site_settings.trickplay_enabled` (`trickplayEnabled()` in `site-settings.ts`, **Trickplay** card on `/admin/settings` with its own `?/updateTrickplay` action) — checked by `attachTrickplay` and `startTrickplayBulk` (`'disabled'` outcome; the Devices button is disabled with a hint); geometry lives on the in-memory `HotSession` (no DB column). Routes under the auth short-circuit: `/api/stream/<id>/trickplay/index.vtt` (`private, max-age=3600`) and `/[sheet].jpg` (buffered, `private, max-age=86400, immutable`; not-ready → `404 no-store`). Player: `trickplaySrc` prop → `<track kind="metadata" label="thumbnails">` on the `<video>` (mode forced to `hidden` in `applyTrackModes`) read by `<media-slider-thumbnail>` inside `.slider-preview`, above the `.slider-time` pill.

Dated summary of what changed and why: `docs/changelog.md`.

### Hard-won gotchas (do not regress)

- Navbar layout: the avatar `DropdownMenu.Trigger` renders through a `child` snippet, so utility classes must go on the rendered `<button>`, not on `Trigger` — a `class` on Trigger is overridden by the button's own `class`. The far-right cluster (Watchlist link + avatar) is a wrapper `<div class="ml-auto …">`; the same rule applies to the card ⋮ trigger in `card-menu.svelte`.
- **`@videojs/html` is beta**: seek buttons, fullscreen, and keyboard shortcuts are deliberately implemented against the native `<video>`/DOM instead of library elements (its store snapshots/fallbacks are buggy). Keep the exact version pin. HLS goes through hls.js whenever `Hls.isSupported()`; native `<video src=m3u8>` only where MSE is missing (Safari/iOS) — Edge answers `canPlayType('application/vnd.apple.mpegurl')` with "maybe" and then fails with `DEMUXER_ERROR_COULD_NOT_PARSE`.
- The fullscreen-only `filter: brightness(1.001)` on the video defeats a Chromium hardware-overlay bug that paints video above DOM controls. Don't remove it.
- ESLint enforces `svelte/no-navigation-without-resolve`: build hrefs via `resolve()` (see `mediaHref`/`watchHref`); vendored `src/lib/components/ui/**` is exempted in `eslint.config.js`.
- Icons: **hugeicons only** (`components.json` iconLibrary); shadcn-svelte style is **maia**.
- `page-gutter` utility (layout.css) = shared horizontal page padding incl. `scroll-padding` for snap carousels — use it, don't hand-roll `px-*` gutters.
- Player top bar lives **inside** `<media-container>` (hover = player activity; visible in fullscreen). Carousel arrows are positioned via `top` offsets, not `-translate-y-1/2` (Button's `active:translate-y-px` overwrites translate).
- The player's source effect reads `videoSrc`/`videoKind`/`startAt` through `$derived`s so a parent that re-creates its `playback` object with equal values can't re-source the video (that refreshes it and seeks back to the resume point). Watch pages still update `playback.subtitles` **in place** after a download rather than spreading a new object.
- Episode→episode navigation stays on the same route, so the **page component** is reused (EpisodesPanel closes via `onNavigate`); the player itself unmounts briefly between episodes while the new playback session is created (`{#if playback}`).
- The controls library has no pin-open API: while `EpisodesPanel` is open, a `.menu-open` class on `.player-root` CSS-overrides the library's `data-visible` auto-hide (and `chromeVisible` keeps top bar/cursor shown). Panel and countdown pill must live inside `<media-container>` to be visible in fullscreen.
- **ffmpeg's mov/hls muxer rebases fmp4 timestamps to 0 every run** — `-copyts`/`-output_ts_offset` do NOT survive into segment `tfdt`. After a seek-restart the gateway binary-patches each segment's tfdt (+startSegment×4s per track timescale, `packages/storage-gateway/src/transcode/mp4-patch.ts`); hls.js/MSE place fragments by tfdt, so don't remove this. Segment grid: 4s fmp4, `-force_key_frames expr:gte(t,n_forced*4)`; the hub synthesizes the full VOD playlist from probed duration (instant seekbar).
- HLS v1 always re-encodes (libx264 veryfast + aac) to **8-bit H.264 High yuv420p** — level 4.1 up to 1080p, 5.2 above, ≤3840 wide (`-pix_fmt yuv420p -profile:v high -level <plan>`). Without the pix_fmt pin, 10-bit HEVC sources (most x265 MKVs) silently produce H.264 High 10, which browsers' MSE reject — ffmpeg exits 0 and the player spins forever. The level/CODECS pair comes from `transcodePlan()` in `src/lib/playback-quality.ts` (hub sends the plan in `session.start`; the master playlist's `CODECS` uses `plan.codec`) — keep gateway flags and plan in sync. HDR sources (PQ/HLG, detected per session via `probeColorTransfer`) are tone-mapped to BT.709 with `zscale`+`tonemap` (needs libzimg — present in the bundled static build). ffmpeg's own `playlist.m3u8` is the gateway's segment-readiness signal, so `hlsArgs` uses `-hls_playlist_type event` — with `vod`, ffmpeg writes that playlist only on exit and every `hls.get` times out. A `-c copy` remux fast-path is deliberately absent: stream-copy splits segments on existing keyframes and would break the fixed grid the synthesized playlist assumes.
- Keep the `/api/stream/` short-circuit FIRST in `hooks.server.ts` — segment fetches arrive every few seconds per viewer and must not hit Better Auth's DB session lookup.
- `fs.stat().mtimeMs` is fractional; DB `bigint` columns reject it (round before insert). `watch_progress` and `watchlist` use partial unique indexes — `onConflictDoUpdate` needs the matching `targetWhere` (a bare `onConflictDoNothing()` needs none). A **multi-row** upsert (`markWatched` for a series) must `set` from `sql\`excluded.*\``— a plain object writes one value to every conflicting row.`markWatched`staggers`updated_at` per episode so the last episode is the newest row (`loadProgress`is newest-first →`playTarget`shows "Play", not "Resume S1E1").`desc()`on a nullable column puts NULLs first in Postgres —`bestMovieFile`orders with`nulls last`.
- `PosterCard` is a `<div class="group relative">` wrapping the `<a>` plus the ⋮ `CardMenu` button as a sibling — interactive content inside `<a>` is invalid HTML, so don't move the menu into the link. `DropdownMenu.Content` is portalled (not in SSR HTML; not clipped by the carousel scroller). `card-menu.svelte` and `detail-hero.svelte` read `page.data.user` to decide whether to render account-only actions; after a toggle they `invalidate('app:watchlist')` (only `/watchlist`'s loader depends on it) and after a dismiss/mark `invalidateAll()`.
- **No DB-level defaults on array columns** (`cast_members`/`genres` use runtime `.$default(() => [])`): drizzle-kit introspects an empty-array DDL default as `'{""}'`, so any `.default([])`/`sql` form makes every `db:push` re-propose the same ALTERs forever.
- The hub is **single-process by design**: `GatewayRegistry` + `SessionManager` + the search index are in-memory (revisit before any horizontal scaling).
- **Search gotchas**: the MiniSearch index only refreshes through `invalidateSearchIndex()` — call it after any bulk catalog write (scan finalize, prune, enrichment already do); a rebuild is cheap (two narrow selects), so there is no incremental path. `processTerm` (diacritic strip) applies to index and query alike — never set `searchOptions.processTerm` separately or "amelie" stops matching "Amélie". Numeric terms are never fuzzed (a year must match exactly) and ≤3-char terms are prefix-only. In `SearchBox`, the listbox swallows `mousedown` so the input keeps focus until a row's click lands (Safari doesn't focus anchors → `focusout` would close the list first), `Escape` is `preventDefault`ed (Chrome clears `type=search` inputs on Escape), and rows stay real `<a>`s so modifier/middle clicks open new tabs. `mediaHref` accepts `Pick<MediaItem,'kind'|'id'>` so `SearchResult`s can link. `/search` re-orders `inArray` results by the hit list (`getMoviesBySlugs` returns DB order).
- TMDB enrichment (`src/lib/server/metadata`) never rewrites `slug` or `hue`/`hue2` (slug = URL + ingest key). Both matched and unmatched titles get `metadata_updated_at` stamped, so scans don't re-search misses; only "Refresh all metadata" (force) retries. It also stores cast headshots (`cast_people` jsonb: name/character/photoUrl), season posters, episode stills, movie budgets and a YouTube `trailer_key` (`pickTrailer` in `map.ts`: trailer > teaser, official, English; `DetailHero` shows **Watch trailer** → `TrailerDialog` YouTube embed only when set). Artwork is hotlinked from `image.tmdb.org` (no hub-side cache); the settings page carries the required TMDB attribution.
- Filename parser (`catalog/parse.ts`): **movies** — the filename decides when it carries a year, else the nearest folder with one (`Inception (2010)/movie.mkv`), else the filename title stands. Year = bracketed `(2017)`/`[2017]` first, otherwise the _last_ plausible bare year before the release junk (`Blade.Runner.2049.2017` → 2017, title keeps 2049); plausible = 1888..next year, so `Blade Runner 2049.mkv` gets no year. **Series** — the `SxxEyy`/`1x02` marker comes from the filename, else the nearest folder (`Show S01E04 - Title/video.mkv`); the show title is the top-level folder cut at the first year / season-pack marker (`S01`, `Season 1-3`, `Complete`) / full episode marker, or the text before the marker for loose files; a missing show year falls back to the text before the marker (`Invincible.2021.S04E01`), never after it. Files whose name or folder says `sample` **and** that probe under 10 min are skipped at ingest (`isSampleFile`) — a feature-length title called "Sample" survives.
- TMDB matching (`pickBestMatch`) **requires a title hit**; the year is only a bonus/tie-break. Ingest stores the file's mtime year when the filename has none, so `findTmdbId` retries without the year whenever the year-filtered search yields no acceptable candidate (TMDB's year filter hides the real title). A forced refresh ("Refresh all metadata") drops the stored `tmdb_id` and re-searches from `scanTitleFromSlug` (the slug is the only field a wrong match can't have overwritten).
- Dev gateway auth: `GATEWAY_DEV_TOKEN` in `.env` self-registers a "Dev Gateway" row (dev mode only; needs one signed-up user first — prefers an admin). Real gateways pair via `/admin/devices` → `npx finderella-storage-gateway pair --hub <url> --code <code>`.
- **Subtitle download hardening**: provider bytes only enter through `safe-fetch.ts` (`fetchSubtitleBytes`: manual redirects, every hop HTTPS on the provider's host allowlist `PROVIDER_HOSTS`, `content-length` + streamed size capped at 2 MiB) — never call `fetch` on a provider-supplied link directly. `zip.ts` filters archive entries by declared size/extension **before** inflating (zip bombs), `validate.ts` `looksLikeSubtitle` rejects HTML/binary/format-mismatched payloads (a Cloudflare page saved as `.srt`), `sanitizeSubtitleText` strips VTT `STYLE`/`REGION` blocks, script tags and control chars; the gateway's `subtitle.put` uses `lstat` (a dangling symlink counts as existing) and `realpath` containment of the parent folder (a symlinked folder can't redirect the write). `/api/subtitles/download` throttles each account to 20 downloads per 10 min.
- **Subtitle download gotchas (Gestdown/Titlovi)**: Gestdown answers **423 with an empty body** both when a show is being refreshed and when it can't parse the language — normalize codes first (`toGestdown`) and never retry in a loop; it has no TMDB lookup and ~50 req/min per IP; ids are strings (`sp_…` season packs download as ZIP). Titlovi: `ExpirationDate` has no zone (treated as local, refreshed a day early), API access is a per-account flag Titlovi can revoke (401 → auth error, not retryable), missing files come back as **HTTP 200 `Wrong parameters.`** (sniff `PK`), archives are windows-1250/1251 and often hold both scripts (`.cyr`/`.cir` entry names → `pickZipSubtitle(..., { script })`), RAR archives are refused with a readable error, a 429 pauses the provider 5 min. Candidate ids for Titlovi are `<mediaid>:<type>`.
- **Subtitle download gotchas**: OpenSubtitles search params must be **alphabetically sorted, lowercase, without default values** (`buildOpenSubtitlesSearchParams`, tested) or the API answers 301 — and `fetch` follows redirects anyway; **406** on `/download` is the daily quota (`remaining: -1`, `reset_time_utc`), not a content-negotiation error; 5 downloads/IP/day without an account, 20+/day with one — the `Test connection` button reports the remaining quota. Subdl codes are uppercase with its own spellings (`BR_PT`, `ZH_BG`); `unpack_files[].url` is a raw file, everything else a ZIP; a shared free key powering a multi-user service breaches Subdl's ToS (the admin page says so). `subtitle.put` refuses non-subtitle extensions, missing folders and existing files (`exists`) unless `overwrite`. A GET `+server.ts` under `/admin/**` is **not** gated by the hook or the layout — `admin/subtitles/status` checks `isAdmin` itself. The bulk job only counts a `not_found` when at least one provider actually answered; every-provider-errored items are `failed` and retried next run.
- **Subtitle gotchas**: `video::cue` accepts only a whitelist of properties (color, font, font-size, text-shadow, background, outline…) — custom properties set on an ancestor do inherit into the cue shadow tree, which is how `cueStyle()` reaches it; `font-size` there is relative to the browser's video-height-scaled cue size, hence the percentage rungs. `@finderella/protocol/languages` is a separate export so the settings page can list languages without pulling zod/message schemas into the client bundle. browsers render only WebVTT in `<track>`, so conversion happens on the gateway (the hub has no ffmpeg). Key `<track>` elements by `track.id`, never `srclang` (English + English SDH share it → `each_key_duplicate`). Drive modes through our `<track>` element refs, never `video.textTracks[i]`, and pass `enableCEA708Captions: false` to hls.js — libx264 keeps broadcast CEA-608/708 data and hls.js would otherwise add phantom "English/Spanish" tracks. hls.js 1.7 never touches `<track>`-element tracks; the `@videojs/html` store only writes modes through its captions elements, which the player no longer renders. `-map 0:<absolute index>` (not `0:s:N`): the ordinal would shift if the text-codec filter changed. Don't feed ffmpeg the original sidecar: decode in Node first (`Buffer.isUtf8` → windows-1252 fallback); `-sub_charenc` needs an iconv build the static binaries may lack. Extraction demuxes the whole container, so a first request on a big MKV can be slow and a forced track can be silent for minutes — the hub route adds a blank-line keepalive every 15 s (legal between VTT blocks; nginx/Cloudflare cut silent bodies at 60–100 s) and `openByteStream`'s `firstByteTimeoutMs` (20 s) fails fast when a pre-subtitle gateway ignores `subtitle.get` (they log and never answer; `GatewayCapabilities.subtitles` gates the route with a 501 too). Subtitle streams don't count against `maxConcurrentTransfers` (a minutes-long one would make segment fetches "device busy" → fatal hls.js error); ffmpeg extractions are capped at 2 separately. `PROTOCOL_VERSION` stays 1 — a bump disconnects every gateway; the new message/fields are additive.
- **Roles / admin gotchas**: every admin `auth.api.*` call (`listUsers`, `setRole`, `banUser`, `removeUser`, `createUser`…) must pass `headers: event.request.headers` — `createUser` without headers silently skips the permission check. `gateway.paired_by_user_id` / `gateway_pairing_code.created_by_user_id` reference `user.id` **without cascade**: the Users page reassigns gateways to the acting admin and deletes the target's codes before `removeUser`. The plugin refuses banning/removing yourself; the app additionally refuses changing your own role and demoting/banning/removing the last active admin. Closed registration is enforced in `auth.ts` `hooks.before` on `/sign-up/email` (covers both the `/register` form action and the raw `/api/auth/sign-up/email` endpoint; always open while the `user` table is empty). Don't `error()` for `/admin` page requests inside `hooks.server.ts` — an error thrown there renders SvelteKit's bare fallback page; the hook only refuses non-GET requests and `src/routes/admin/+layout.server.ts` throws the 403 that renders `+error.svelte`. `sveltekitCookies` must still be the last plugin; after changing auth plugins run `npm run auth:schema` then `npm run db:generate`.
- **Trickplay gotchas**: `<media-thumbnail>`/`<media-slider-thumbnail>` has **no `src`** — it reads cues from the player store's text-track feature (`kind === 'metadata' && label === 'thumbnails'` on a `<track>` of the `<video>`), and `#xywh` URLs resolve relative to the track `src`. A metadata track's cues only load in `hidden`/`showing` mode, so `applyTrackModes` forces `hidden` (stamping `modesAppliedAt`, otherwise the `change` echo reads as "viewer switched subtitles off"). The library writes inline `position/left/width` on `<media-slider-preview>` (`getSliderPreviewStyle`) — never add `left`/`translate` to `.slider-preview` (it double-shifts). The thumbnail element re-assigns `img.src` only when the resolved URL changes, so a 404'd sheet retries when the pointer crosses into another sheet and back. The sheet route buffers the JPEG before answering so a gateway failure can't become a cached broken `200`; errors stay `no-store`. Geometry is the **gateway's**: explicit `scale=W:H` from the display aspect (`-2` would ignore SAR), and the hub never derives tile size. `-skip_frame nokey` + `fps` means a tile can be up to half a GOP off (fine for previews). `-atomic_writing` + a JPEG-EOI check make partially written sheets unservable; a cache dir without `manifest.json` is a stale partial and is wiped. Never route `trickplay.get` through `startTransfer` (it must not make segment fetches "device busy"). Ensure results are best effort: a null/failed reply means `trickplay: null`, playback unaffected. Old gateways ignore the messages (they log) — the capability flag and `firstByteTimeoutMs` guard the routes. A future "clear thumbnail cache" belongs on the gateway (`rm -rf trickplayRoot()` behind a new message).

## Commands

- `npm run dev` — start dev server (`-- --open` to open browser); serves `/gateway/ws` via a Vite plugin
- `npm run build` / `npm run preview` — production build / preview it
- `npm run start` — run the production build via `server/index.js` (adapter-node handler + gateway WS upgrades)
- `npm run gateway:dev -- connect [--hub URL] [--token T]` — run the storage gateway (media agent) from source (env: `FINDERELLA_HUB`, `FINDERELLA_TOKEN` or dev `GATEWAY_DEV_TOKEN`)
- `npm run test` — vitest (protocol framing, filename parser, mp4 tfdt patching, HLS ffmpeg args, trickplay geometry / ffmpeg args / VTT synthesis, subtitle sidecar matching / embedded mapping / SRT→VTT, TMDB mapping, search engine ranking)
- `npm run check` — typecheck with svelte-check (`check:watch` for watch mode)
- `npm run lint` — prettier check + eslint; `npm run format` to auto-format
- `npm run db:push` — push schema changes directly to the database (dev workflow; migrations in `drizzle/` are for fresh prod bootstrap via `db:migrate`)
- `npm run db:generate` / `npm run db:migrate` — generate SQL migrations / apply them
- `npm run db:studio` — open Drizzle Studio
- `npm run auth:schema` — regenerate `src/lib/server/db/auth.schema.ts` from the Better Auth config in `src/lib/server/auth.ts` (run after changing auth plugins/options)

Verify with `npm run test`, `npm run check`, `npm run format && npm run lint`, and dev-server smoke tests.

A `.env` file is required (see `.env.example`): `DATABASE_URL` (PostgreSQL), `ORIGIN`, `BETTER_AUTH_SECRET`, and optionally `GATEWAY_DEV_TOKEN` (dev-only shared gateway token) and `TMDB_API_KEY` (metadata/artwork; v4 read token or v3 key). `drizzle.config.ts` and the db client both throw if `DATABASE_URL` is unset.

## Architecture

SvelteKit 2 + Svelte 5 (TypeScript), Tailwind CSS v4 (via the Vite plugin, no tailwind config file — theme lives in `src/routes/layout.css`), Drizzle ORM on postgres.js, Better Auth for authentication, **adapter-node** (custom prod server `server/index.js` owns the `/gateway/ws` WebSocket upgrade, bridged from the SvelteKit bundle via the `init` hook's `globalThis.__finderellaGatewayUpgrade`). npm workspaces: repo root is the web app; `packages/protocol` (shared zod schemas, exports source TS — bundled via `ssr.noExternal`) and `packages/storage-gateway` (CLI). Runtime deps live in `dependencies` (not devDependencies) so `npm ci --omit=dev` works on a VPS.

**Svelte runes mode is forced** for all project files via `vite.config.ts` compiler options — use `$state`, `$derived`, `$props`, etc.; legacy reactive syntax will not compile. There is no `svelte.config.js` — SvelteKit config lives inline in `vite.config.ts` (which also sets `ssr.noExternal` for `@hugeicons/svelte`).

Theme tokens (oklch, dark forced via `class="dark"` in `src/app.html`) and custom utilities (`page-gutter`, `scrollbar-none`, `hero-fade-*`) live in `src/routes/layout.css` — don't move it (components.json / prettier point at it).

### Server-side layout (`src/lib/server/`)

- `db/index.ts` — exports the `db` Drizzle client, initialized from `$env/dynamic/private`.
- `db/schema.ts` — barrel re-exporting `auth.schema.ts` + `catalog.sql.ts` (movie/series/season/episode) + `gateways.sql.ts` (gateway/pairing-code/library/media_file/media_subtitle) + `playback.sql.ts` (playback_session, watch_progress) + `site.sql.ts` (site_settings singleton) + `watchlist.sql.ts` (watchlist) + `user-settings.sql.ts` (per-account subtitle settings) + `subtitles.sql.ts` (subtitle_settings singleton, subtitle_download log). Drizzle Kit reads only this file.
- `db/auth.schema.ts` — **generated** Better Auth tables. Don't hand-edit; regenerate with `npm run auth:schema`.
- `auth.ts` — Better Auth instance (`better-auth/minimal`), Drizzle adapter, email/password, `admin()` plugin (roles/bans), first-user-is-admin database hook, registration-closed `hooks.before`. The `sveltekitCookies` plugin must remain last.
- `site-settings.ts` — `site_settings` singleton (`getSiteSettings`/`updateSiteSettings`), `countUsers`, `registrationStatus`; `users.ts` — `listActiveAdmins`/`isLastActiveAdmin`/`ensureAdminExists`.
- `gateways/` — `ws.ts` (upgrade auth + message dispatch), `registry.ts` (live connections, request/resp correlation, credit-backpressured byte streams), `scan.ts` (trigger library scans).
- `catalog.ts` / `catalog/{parse,ingest}.ts` — catalog queries returning frontend shapes; filename→metadata parsing and scan ingestion.
- `subtitles/` — provider settings, OpenSubtitles/Subdl clients, search + ranking, install (gateway `subtitle.put` + rows), bulk/auto job runner.
- `trickplay/` — seek-bar thumbnails: `ensure.ts` (gateway `trickplay.ensure` request), `vtt.ts` (synthesized thumbnail track), `bulk.ts` (admin per-library job).
- `streaming/` — `compat.ts` (direct-play matrix), `source-picker.ts` (best online file), `session-manager.ts` (hot sessions, idle reaper, orphan cleanup at boot), `hls-playlist.ts` (synthesized VOD playlists), `subtitles.ts` (per-session `SubtitleTrack` list + labels).
- `metadata/` — TMDB enrichment (`tmdb.ts` client, `map.ts` mapping, `index.ts` runner + `metadataStatus`).
- `search/` — MiniSearch catalog index (`engine.ts` pure ranker, `index.ts` process-wide singleton with lazy, coalesced rebuilds behind `invalidateSearchIndex`).
- `progress.ts` — watch positions, continue-watching (+ dismiss), mark-as-watched, and the per-viewer overlay (progress + watchlist membership); `watchlist.ts` — saved titles; `log.ts` — pino.

### Auth flow

`src/hooks.server.ts`: `/api/stream/*` short-circuits before any auth (capability-uuid sessions); everything else resolves the Better Auth session into `event.locals` (typed from `auth.$Infer` in `app.d.ts`, so `user.role`/`banned` are available). Unauthenticated requests: always public are `/login`, `/register`, `/api/auth/*`, `/api/gateway/pair`; `ACCOUNT_PREFIXES` (`/settings`, `/logout`, `/admin`, `/watchlist`) always need a session; everything else depends on **`site_settings.require_login`** (`loginRequired()` in `site-settings.ts`, cached 5 s in-process and cleared on update) — on, redirect to `/login` (pages) or a readable `401` (`/api/*`, so the player shows the message instead of following a redirect into HTML); off = **guest mode**: browse + watch without an account. Guest support: `playback_session.user_id` is nullable (`sessionManager.start(null, …)`), the progress helpers (`loadProgress`/`withProgress`/`continueWatching`/`*ResumePosition`) accept `userId: string | null` and return empty results, loaders pass `locals.user?.id ?? null`, `/api/progress` answers `{ ok: false }` for guests, `/api/playback/stop` matches owner `null`, logout lands on `/` instead of `/login`, and `SiteHeader` takes `user: HeaderUser | null` (guests get a single **Sign in** button). Non-admin non-GET requests under `/admin` get a 403 in the hook, admin page requests are 403'd by `src/routes/admin/+layout.server.ts`. `/login` (shadcn `login-01` block → `src/lib/components/login-form.svelte`) and `/register` (`signup-01` block → `signup-form.svelte`; shows "Create the admin account" on an empty hub and a "Registration is closed" card when the switch is off) are form actions calling `auth.api.*`; sign-out is the `POST /logout` action (navbar avatar menu, mobile sheet, admin sidebar footer). The root `+layout.server.ts` exposes `{ user: { name, email, image, isAdmin } | null }`; the root layout renders the navbar only when signed in and outside `/admin`. Gateways authenticate with bearer tokens on the WS upgrade — never cookies.
